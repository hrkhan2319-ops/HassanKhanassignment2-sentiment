# Tested with Python 3.12 (CPU-only). Any recent versions of these work;
# the pins below are the versions used to produce the submitted results.
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
torch>=2.1
gensim>=4.3
nltk>=3.8
matplotlib>=3.7
jupyter
